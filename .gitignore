node_modules
.svelte-kit
.DS_Store
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local staging folder for new note images — the canonical copies live
# in static/notes/. Ignored to keep the repo small.
Pictures/
